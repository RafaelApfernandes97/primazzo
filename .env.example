# ====================
# Configuração do MongoDB
# ====================
MONGODB_URI=mongodb://usuario:senha@host:porta/?tls=false&authSource=admin&authMechanism=SCRAM-SHA-256
MONGODB_DATABASE=suite

# ====================
# Configuração do Servidor
# ====================
PORT=3001
NODE_ENV=production

# ====================
# Autenticação JWT
# ====================
JWT_SECRET=sua_chave_secreta_super_segura_aqui

# ====================
# Configuração do MinIO/S3
# ====================
# Endpoint da API S3 (não usar 'console-' no nome)
MINIO_ENDPOINT=outros-minio.8a2h9d.easypanel.host
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=sua_access_key_aqui
MINIO_SECRET_KEY=sua_secret_key_aqui
MINIO_BUCKET=primazzo
MINIO_REGION=us-east-1

# ====================
# Notas de Configuração
# ====================
#
# MongoDB:
# - Use a URI completa de conexão do MongoDB
# - Certifique-se de que o usuário tem permissões adequadas
#
# MinIO:
# - O endpoint deve ser o da API S3, não da console web
# - Se usar Easypanel, remova o prefixo 'console-' do hostname
# - Exemplo: console-minio.host -> minio.host ou outros-minio.host
#
# JWT:
# - Use uma chave forte e única para produção
# - Nunca compartilhe ou commite a chave real
#
# Porta:
# - A porta 3001 é a padrão, mas pode ser alterada conforme necessário
# - No Easypanel, geralmente usa-se a porta configurada no painel
